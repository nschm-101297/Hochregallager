<Window x:Class="WarehouseManagementSystem.Views.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:WarehouseManagementSystem.Views"
        mc:Ignorable="d"
        Background="Beige"
        WindowStartupLocation="CenterScreen"
        WindowState="Maximized"
        Title="WarehouseManagementSystem" Height="450" Width="800">
    <Window.Resources>
        <Style TargetType="Label">
            <Setter Property="Background" Value="Transparent"/>
            <Setter Property="VerticalContentAlignment" Value="Center"/>
            <Setter Property="FontWeight" Value="Bold"/>
        </Style>
        <Style TargetType="Button">
            <Setter Property="Background" Value="Transparent"/>
            <Setter Property="Margin" Value="5"/>
        </Style>
    </Window.Resources>
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="30"/>
        </Grid.RowDefinitions>
        <Label Grid.Row="0"
               HorizontalContentAlignment="Center"
               FontSize="20"
               Content="Header"/>
        <Border Grid.Row="1"
                Height="2"
                Background="Black"
                HorizontalAlignment="Stretch"/>
        <ContentPresenter Grid.Row="2"/>
        <Button Grid.Row="3"
                HorizontalAlignment="Left">
            <Image Source="/Images/home.png" Width="70"/>
        </Button>
        <StatusBar Grid.Row="4"
                   HorizontalContentAlignment="Stretch">
            <Label Content="Instructions/infos"
                   HorizontalContentAlignment="Left"
                   FontSize="10"/>
        </StatusBar>
    </Grid>
</Window>
